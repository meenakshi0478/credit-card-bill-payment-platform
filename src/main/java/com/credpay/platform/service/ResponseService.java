package com.credpay.platform.service;

import java.util.HashMap;
import java.util.Map;

public class ResponseService {
   /* public Object ResposeService(HashMap data, int statusCode, String statusMessage) {
        Map obj2 = new HashMap();
        obj2.put("status", statusCode);
        obj2.put("message", statusMessage);
        obj2.put("responseData", data);
        return obj2;
    }*/
}
